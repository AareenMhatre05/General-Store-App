org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Kotlin's incremental compiler cannot close its caches on this Windows
# setup -- every plugin's compileDebugKotlin task dies with
# "Could not close incremental caches in ...\caches-jvm\jvm\kotlin".
# Incremental compilation only saves time on rebuilds; correctness beats
# it. Remove this if a future toolchain stops hitting the bug.
kotlin.incremental=false
