# No app-specific keep rules are needed at the moment. The Flutter
# Gradle plugin contributes its own rules for the engine and plugins.
#
# If a release build ever crashes in a way a debug build doesn't, R8
# stripping something reflective is the first thing to suspect -- add a
# -keep rule for the offending package here rather than turning
# minification off wholesale.
